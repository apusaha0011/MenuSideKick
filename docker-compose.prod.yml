version: "3.9"

services:
  backend:
    image: shemanto27/menu-sidekick:backend-latest
    container_name: menusidekick-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
