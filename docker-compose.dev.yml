version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: menusidekick-backend
    container_name: menusidekick-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
